---
import BaseLayout from '../layouts/BaseLayout.astro';
import SectionTitle from '../components/SectionTitle.astro';
import Button from '../components/Button.astro';
---
<BaseLayout
  title="Contact — ELINA B - Esthétique & formations"
  description="Contactez-nous pour vos prestations, formations ou demandes personnalisées."
>
  <section class="container-shell py-20">
    <div class="grid gap-12 lg:grid-cols-[0.55fr_0.45fr]">
      <div class="space-y-8">
        <SectionTitle
          eyebrow="Contact"
          title="Parlons de votre projet beauté"
          subtitle="Nous répondons rapidement à toute demande de prestation, formation ou collaboration."
        />
        <form id="contact-form" class="grid gap-5" method="post">
          <div class="grid gap-4 sm:grid-cols-2">
            <label class="text-xs font-semibold uppercase tracking-[0.3em] text-cocoa/70">
              Nom
              <input
                type="text"
                name="lastName"
                required
                class="mt-2 w-full rounded-xl border border-sand/60 bg-white/80 px-4 py-3 text-sm focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-gold/70"
              />
            </label>
            <label class="text-xs font-semibold uppercase tracking-[0.3em] text-cocoa/70">
              Prénom
              <input
                type="text"
                name="firstName"
                required
                class="mt-2 w-full rounded-xl border border-sand/60 bg-white/80 px-4 py-3 text-sm focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-gold/70"
              />
            </label>
          </div>
          <label class="text-xs font-semibold uppercase tracking-[0.3em] text-cocoa/70">
            Email
            <input
              type="email"
              name="email"
              required
              class="mt-2 w-full rounded-xl border border-sand/60 bg-white/80 px-4 py-3 text-sm focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-gold/70"
            />
          </label>
          <label class="text-xs font-semibold uppercase tracking-[0.3em] text-cocoa/70">
            Téléphone (optionnel)
            <input
              type="tel"
              name="phone"
              class="mt-2 w-full rounded-xl border border-sand/60 bg-white/80 px-4 py-3 text-sm focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-gold/70"
            />
          </label>
          <label class="text-xs font-semibold uppercase tracking-[0.3em] text-cocoa/70">
            Sujet
            <select
              name="subject"
              required
              class="mt-2 w-full rounded-xl border border-sand/60 bg-white/80 px-4 py-3 text-sm focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-gold/70"
            >
              <option value="Prestations">Prestations</option>
              <option value="Formations">Formations</option>
              <option value="Autre">Autre</option>
            </select>
          </label>
          <label class="text-xs font-semibold uppercase tracking-[0.3em] text-cocoa/70">
            Message
            <textarea
              name="message"
              rows="5"
              required
              class="mt-2 w-full rounded-xl border border-sand/60 bg-white/80 px-4 py-3 text-sm focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-gold/70"
            ></textarea>
          </label>
          <label class="flex items-start gap-3 text-xs text-cocoa/80">
            <input type="checkbox" required class="mt-1 h-4 w-4 rounded border-sand/60" />
            J'accepte que mes données soient utilisées pour me recontacter dans le cadre de ma demande.
          </label>
          <div class="flex flex-wrap gap-4">
            <Button type="submit" variant="primary">Envoyer ma demande</Button>
            <p id="form-success" class="hidden text-sm text-cocoa/80">
              Merci, votre message a bien été envoyé. Nous revenons vers vous rapidement.
            </p>
          </div>
        </form>
      </div>
      <aside class="space-y-6 rounded-2xl border border-sand/60 bg-white/70 p-6 shadow-soft">
        <div>
          <p class="text-xs font-semibold uppercase tracking-[0.3em] text-cocoa/70">Coordonnées</p>
          <p class="mt-3 text-sm text-cocoa/80">contact@elina-b.fr</p>
          <p class="text-sm text-cocoa/80">+33 6 12 34 56 78</p>
        </div>
        <div>
          <p class="text-xs font-semibold uppercase tracking-[0.3em] text-cocoa/70">Horaires</p>
          <p class="mt-3 text-sm text-cocoa/80">Lun–Sam : 09:00 – 19:30</p>
          <p class="text-sm text-cocoa/80">Sur rendez-vous uniquement</p>
        </div>
        <div>
          <p class="text-xs font-semibold uppercase tracking-[0.3em] text-cocoa/70">Adresse</p>
          <p class="mt-3 text-sm text-cocoa/80">3 rue des Jasmins, 31000 Toulouse</p>
        </div>
      </aside>
    </div>
  </section>
  <script>
    const form = document.querySelector('#contact-form');
    const success = document.querySelector('#form-success');
    if (form && success) {
      form.addEventListener('submit', (event) => {
        event.preventDefault();
        success.classList.remove('hidden');
        form.reset();
      });
    }
  </script>
</BaseLayout>
